#!/usr/bin/env bash
# Rollback do rice em um comando: volta para a Waybar (setup original).
# Para reativar o rice: ~/.config/scripts/rice-mode.sh quickshell
exec "$HOME/.config/scripts/rice-mode.sh" waybar
